package com.husc.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.husc.models.ListImagePlace;

public interface ListImagePlaceRepository extends JpaRepository<ListImagePlace, Long> {
}
